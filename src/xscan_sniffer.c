#include "xscan_sniffer.h"

void * scan_sniffer( void *st )
{
    struct xp_stats *stats = (struct xp_stats *) st;
    /* printf( "%d\n", setup.pid );
    printf( "%d\n", setup.type );
    printf( "%d\n\n", setup.tty );
    printf( "%d\n", setup._ports.range );
    printf( "%d\n", setup._ports.start );
    printf( "%d\n", setup._ports.end );
    printf( "%d\n", setup.verbose ); */
    return NULL;
}